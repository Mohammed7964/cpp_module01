#ifndef REPLACE_H
#define REPLACE_H
#include <iostream>

#include <string>

std::string replace_all(const std::string &content,
                        const std::string &s1,
                        const std::string &s2);

#endif